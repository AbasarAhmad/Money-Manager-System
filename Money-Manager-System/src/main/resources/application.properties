spring.application.name=Money-Manager-System

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
server.port=${SERVER_PORT}


server.servlet.context-path=/api

# Hibernate/JPA Config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


# ==========================
# Email Configuration
# ==========================
spring.mail.host=${MAIL_HOST}
spring.mail.port=${MAIL_PORT}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.protocol=smtp
spring.mail.from=${MAIL_FROM}

# Debugging (optional)
spring.mail.debug=true

# JWT secret key (use your own long random string)
jwt.secret=3e0a7d9f9b89a5b4e1c2d8f3f7a0b4c9d4f2e8a7c1d3b5a9f7e6d1b0a2c3e4f9


# Fronted url
money.manager.frontend.url=http://localhost:5173

